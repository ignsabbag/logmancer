[package]
name = "logmancer-tui"
version = "0.4.1"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
logmancer-core = { path = "../logmancer-core", features = ["native-logging"] }

# Logging
log = "0.4"
anyhow = "1.0"
directories = "6"

[[bin]]
name = "logmancer-tui"
path = "src/main.rs"
